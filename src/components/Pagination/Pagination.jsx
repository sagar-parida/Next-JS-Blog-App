import React from 'react'
import styles from './Pagination.module.css'

const Pagination = () => {
    return (
        <div styles={styles.container}>Pagination</div>
    )
}

export default Pagination